# --- app ---
APP_VERSION=dev

# --- database ---
DATABASE_URL=postgresql://doadmin:${db_password}@${postgres_host}:${postgres_port}/defaultdb?sslmode=require

# --- CORS / web ---
CORS_ORIGIN=https://${app_domain}

# --- Caddy / reverse proxy ---
CADDY_EMAIL=${caddy_email}
APP_DOMAIN=${app_domain}
