version: "3.8"
services:
  web:
    image: ${web_image}
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
  api:
    image: ${api_image}
    restart: unless-stopped
    environment:
      DATABASE_URL: "${database_url}"
    ports:
      - "127.0.0.1:3001:3001"
