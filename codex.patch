diff --git a/apps/web/app/api/cover-letters/route.ts b/apps/web/app/api/cover-letters/route.ts
index 1af5c36d51438597afe8fc93eef40403e428053f..b65a77937e717b189af229212e9f38e71bb7a2ff 100644
--- a/apps/web/app/api/cover-letters/route.ts
+++ b/apps/web/app/api/cover-letters/route.ts
@@ -1,45 +1,47 @@
 import { NextRequest, NextResponse } from "next/server";
 
 import { getApiBaseUrl, relayApiResponse, requireAuthToken } from "../baselines/helpers";
 
 export async function GET(req: NextRequest) {
   const baseUrl = getApiBaseUrl();
   const { token, error } = requireAuthToken(req);
 
   if (!baseUrl) {
     return NextResponse.json(
       { error: "API base URL is not configured" },
       { status: 500 },
     );
   }
 
   if (!token) {
     return error;
   }
 
-  const response = await fetch(`${baseUrl}/cover-letters`, {
+  const queryString = req.nextUrl.search ?? "";
+
+  const response = await fetch(`${baseUrl}/cover-letters${queryString}`, {
     method: "GET",
     cache: "no-store",
     headers: {
       Authorization: `Bearer ${token}`,
     },
   });
 
   return relayApiResponse(response);
 }
 
 export async function POST(req: NextRequest) {
   const baseUrl = getApiBaseUrl();
   const { token, error } = requireAuthToken(req);
 
   if (!baseUrl) {
     return NextResponse.json(
       { error: "API base URL is not configured" },
       { status: 500 },
     );
   }
 
   if (!token) {
     return error;
   }
 
