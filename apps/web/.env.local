# server-side (Next route handlers, server actions)
# Use the Docker service name so the web container can reach the API container.
# If you run the web app outside Docker, change this to http://localhost:3001.
API_BASE_URL=http://api:3001

# client-side (browser)
NEXT_PUBLIC_API_BASE_URL=http://api:3001
